<%= form_for hero do |f| %>
  <% if hero.errors.any? %>
    <div class="error-message">
      <%= hero.errors.full_messages.to_sentence.capitalize %>
    </div>
  <% end %>
  <%= f.text_field(:name)   %>
  <%#= f.input :name, input_html: { autofocus: true } %>
  <%= f.submit class: "btn btn-secondary" %>
<% end %>